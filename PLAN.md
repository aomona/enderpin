# Enderpin 実装計画

状態: ユーザーの承認を受け実装中。2026-09-14更新。

## 目的

Enderpinを、Minecraftの構成・パッケージ・実行環境・起動を管理する独立したRustライブラリとCLIとして作る。MonaLauncherは同じライブラリを利用するGUIとなる。

中心価値は構成の固定と再現。サーバー管理者と参加者が同じGitリポジトリを使い、クライアントとサーバーそれぞれに必要な環境を再現できることを目指す。まず自分の環境で実用化し、その後に一般配布する。

## 確定した要件

### ワークスペースと共有

- 1つのリポジトリに共通のパッケージ指定とclient/serverそれぞれの指定を持つ。
- クライアントとサーバーは異なるローダーを使える。例: FabricクライアントとPaperサーバー。
- 構成ファイルは `enderpin.toml`、ロックファイルは `enderpin.lock` を基本とする。
- ロックは導入済み台帳ではなく、再現すべき解決済み構成。対象ごとのファイルとハッシュを記録する。
- 共通指定は両側で使用したいパッケージを表す。環境が違えば同じプロジェクトから異なるファイルを選択・固定できる。
- 同一ファイルはキャッシュを共有する。配置先・依存解決・導入済み状態・実行中プロセスは対象ごとに分離する。
- 標準クライアントは共有サーバーへの参加用。シングルプレイ用に必要なMODは明示指定できる。
- Gitの過去の構成とロックへ戻し、`sync --locked`で復元する。独自の更新履歴・rollbackコマンドは初版に含めない。
- MOD設定ファイルやワールドの内容まで再現する要件は現時点で含めない。

### 導入対象と有効・無効

- 最初はMOD・リソースパック・シェーダー。Paper対応段階でプラグインを扱う。
- 配布元の環境情報は初期導入対象の判断材料にする。利用者はclient/serverそれぞれで有効・無効を明示的に上書きできる。
- server-onlyをclientへ、client-onlyをserverへ導入する指定も許容する。有効化は導入対象の指定であり、MODの実行時互換性の保証ではない。
- 個人用ignoreではパッケージ名・IDを指定して、そのPCで除外する。共有の構成・ロックを変更しない。
- ignoreにより有効なパッケージの必須依存が欠ける場合は、依存元と理由を表示して停止する。
- `sync --locked`は共有ロックに個人用ignoreを適用した結果を再現する。元の共有構成を完全に再現する場合はignoreなしで実行する。
- 管理外の既存ファイルは保持する。同名衝突は停止する。
- 管理対象ファイルの手動変更を検出したら停止し、明示的な復元操作で戻す。
- 既存MODフォルダの一括取り込みは初版に含めず、Enderpinで追加して構成を作る。

### 取得元と固定

- Modrinth: 検索・追加・更新・依存解決に対応する。
- GitHub Releases等: 認証不要の公開HTTPSファイルURLを指定して追加する。種類は利用者が明示し、依存パッケージも手動で追加する。
- URL由来の更新は利用者が新しいURLを指定する。GitHub専用の最新版探索・ログインは初版に含めない。
- 導入する実ファイルのハッシュをロックに保存する。固定後にURLの内容が変われば同期を停止し、新しいハッシュを勝手に採用しない。
- ハッシュをキーにした共通ローカルキャッシュを保持し、再利用時も内容を検証する。
- ファイル取得元または保持済みコピーが利用できる場合に再現できる。ハッシュだけから削除済みファイルを復元するものではない。
- キャッシュの持ち運び・共有サービスは後の段階。

### Modrinthの選択と依存

- 標準は対象Minecraftと実行環境に適合する最新の正式版。ベータ版は明示的に許可し、特定バージョンの固定も可能にする。
- 通常同期は固定版を維持する。最新版への更新は `update` で行う。
- 必須依存を自動導入する。任意依存は対話時に選択可能にし、標準は未選択。選択を共有構成に保存し、同期では聞き直さない。
- 非対話利用では任意依存をフラグで指定できる。
- 内包済み依存は別途取得しない。宣言された非互換・依存条件の衝突は理由を示して停止する。
- 初版は過去バージョンの組み合わせを総当たりする解決器を実装しない。
- 同じMinecraft環境に配置できること、依存宣言を満たすこと、実際のゲーム内通信が成立することは別々に検証する。

### CLI

- Rustライブラリと薄いCLI。MonaLauncherはCLIの表示文字列を解析せずライブラリを利用する。
- 通常の `add` / `remove` は構成・ロック・実際の配置まで反映する。
- 手編集後の通常の `sync` は変更を解決して反映する。変更のないパッケージは既存の固定版を維持する。
- `sync --locked` は構成とロックの不一致で停止する。
- `launch` は起動前に `sync --locked` 相当を実行する。不足ファイルは取得するが、構成更新や手動変更の上書きは行わない。
- 検索は対話時に1件選択→詳細表示→追加→任意依存選択→導入。操作対象に応じた検索条件と追加先を表示する。
- 非対話時の検索は一覧出力。`--json` と `--no-interactive` を用意する。
- サーバーは前景で起動し、ログ表示・標準入力のコンソール操作・正常終了に対応する。独自の常駐管理機構は初版に含めない。

### 実行環境と起動

- 最終的にEnderpin単体でクライアントとサーバーを準備・起動する。
- 必要なJavaを自動取得する。Minecraft本体・ローダー・使用するJavaの配布版も固定する。
- OS・CPUごとに異なる実行ファイルは対応するものを取得・検証する。
- クライアント起動の認証情報は共有構成・ロック・Gitに保存しない。
- CLIでもサンドボックス起動に対応する。既存MonaLauncher実装を調査して共有する範囲を決める。
- サンドボックスを既定とする。初版は対象のゲームディレクトリと専用一時ディレクトリへの書込を許可し、共有Javaやライブラリは読取専用。通信は許可・拒否の2択で、オンライン利用時は許可する。
- サンドボックス起動を選んだ場合、利用できないことを理由に黙って通常起動へ切り替えない。
- 任意のMOD権限メタデータ連携は後の段階。OSサンドボックスの提供とは別の機能として扱う。

### Connector

- Sinytra Connectorに対応する。利用は構成で明示し、対象版に必要なConnectorと依存の準備を自動化する。
- 実行するローダーと、導入するMODのローダー種別を別に扱う。
- Connectorを利用するNeoForge環境ではFabric版MODも指定できる。
- 通常のFabric環境とConnector環境ではFabric API等の依存充足方法が異なる。対象版に対応した明示的な処理を用意し、採用したファイルをロックする。
- 任意の互換レイヤーを動的にロードする汎用プラグイン機構は現時点で作らない。
- 対応バージョンや依存セットは実装時に公式情報を再確認する。解決成功を全MODの動作保証として扱わない。

### 対応OS

- Windows・macOS・Linuxを対象に設計する。
- 手元のmacOSで実利用を確認し、Windows・LinuxはCIで検証する。
- ファイル同期のテスト、実際のゲーム起動、サンドボックスの強制検証を区別して報告する。

## 最小構成の提案（最終確認前）

- 最初は独立したRustパッケージのライブラリとCLIから開始する。追加crateは明確な分離理由が生じた時だけ増やす。
- 構成・ロックはTOMLとし、スキーマのバージョンを持たせる。
- Git管理対象は構成・ロック・README等。実行ディレクトリ、導入済み台帳、個人用ignore、ログ、認証状態はGit管理外にする。
- 共通指定を対象別指定へ展開し、対象ごとに解決する。1つのロックに各対象の結果を保持する。
- ファイル名やロック内のフィールド、ignoreのファイル名、実行ディレクトリの名前は具体案を最終確認時に提示する。
- HTTPSのリダイレクト先、入力された配置名・パス、外部メタデータ、アーカイブ展開先を検証する。
- 必要なダウンロードをステージングしてハッシュを確認してから反映する。反映失敗・中断から復旧でき、既存ファイルを失わない方式にする。
- 同じ対象への同時変更と実行中環境へのファイル差し替えを防ぐ。
- 対象を指定した同期・起動で、反対側のファイル取得やインストールを必須にしない。
- ライブラリは表示文字列から独立した結果・エラー・進捗を返し、CLIとGUIで表示する。

## 実装順の案

### 1. 共有構成の固定・同期とCLI

共通/client/server指定、対象別ロック、ignore、Modrinth、URL、ハッシュ検証、キャッシュ、導入・削除・更新・検索UIを実装する。

完了条件: 共有構成とロックから、別々の空ディレクトリへclient/serverの選択結果を再現できる。片側の同期が反対側のファイルに影響せず、Gitで戻したロックから復元できる。

### 2. Fabricの両側管理・起動（最初の実用段階）

Minecraft・Fabric・Javaの準備と固定、クライアント認証、サーバーコンソール、両側の起動、合意した範囲のサンドボックスを実装する。

完了条件: 同じリポジトリを使い、Fabricサーバーを起動し、別のクライアント環境から接続して遊べる。選択したサンドボックスの制限が実際に強制されることを別途確認する。

### 3. Paperサーバー＋Fabricクライアント

Paperの準備・固定・起動、プラグイン導入を追加する。

完了条件: serverはプラグイン、clientはMODを使用する構成を同じリポジトリから再現し、実際に接続できる。

### 4. NeoForge＋Sinytra Connector

NeoForgeの準備・固定・起動、明示的なConnector利用、対応版の依存代替を追加する。

完了条件: 対応する具体的なバージョンとMODの組み合わせで再現・起動を確認する。互換性が確認できた範囲を記録する。

### 5. MonaLauncherから利用

CLIで確認したライブラリをMonaLauncherへ接続する。MonaLauncher固有の画面・状態管理と、共通の環境操作を分離し、現在の保護を保つ。

### 6. 任意の権限メタデータ連携

対象ファイルに対応する外部権限情報を照合する。情報の取得、利用者の承認、OSサンドボックスによる強制を区別して設計する。詳細はこの段階の前に詰める。

## 検証計画

- resolver: 必須・任意・内包済み・非互換、衝突、依存サイクル、ignoreで必須依存が欠ける場合。
- lock: 通常同期の固定版維持、locked時の不一致拒否、個人用ignore、client/serverで異なる配布ファイル、Gitで戻した構成の復元。
- filesystem: 管理外ファイル保持、同名衝突、手動変更、部分ダウンロード、ハッシュ不一致、処理中断と復旧、同時操作、パス逸脱。
- sources: Modrinthの代表的なメタデータ、公開HTTPS URLとリダイレクト、消失した配布元とキャッシュ再利用。
- CLI: 対話検索・任意依存選択、非対話一覧、JSON出力、失敗時に入力待ちにならないこと。
- runtime: OSごとのJava取得、固定版起動、Fabricのclient/server接続、Paperのプラグイン、Connectorの具体的な対応構成。
- sandbox: 起動できることだけでなく、許可した操作と拒否すべき操作を最小プローブで検証する。OS別・client/server別に結果を記録する。
- 通常の自動テストはローカルの固定データ・一時ディレクトリを中心とし、外部サービスへの実接続と実ゲーム起動を分けて実行する。

## 既存コードの調査結果

調査は読取のみ。以下はコード上の経路の確認で、今回のヒアリング中にゲームや検証CLIを実行したものではない。

- MonaLauncherのModrinth APIと導入処理: `src-tauri/src/minecraft/modrinth.rs`、`modrinth_installer.rs`。
- Minecraft本体・Fabric・Javaの準備: `installer.rs`、`fabric.rs`、`runtime.rs`。本体のRust関数はTauriを直接参照しないが、独自のパス・manifest等に依存する。
- 認証HTTP処理: `src-tauri/src/auth/microsoft.rs`、`minecraft_services.rs`。認証状態・秘密の保存とUI側の状態管理には別の結合がある。
- 起動: `src-tauri/src/minecraft/launcher.rs`。サンドボックス、認証broker、MonaLauncherのmanifestに依存し、単純なコピーで独立利用できるものではない。
- 既存CLIからの利用経路: `src-tauri/src/bin/minecraft_smoke.rs`、`linux_minecraft_smoke.rs`。
- 再利用時は各呼び出し経路を確認し、MonaLauncherの現在の制約を取り除いて動かす変更を行わない。

### サンドボックスの追加調査

- 現行MonaLauncherのMinecraft導入・起動はクライアント向け。専用サーバーの完成した起動経路は確認できていない。
- macOSのSeatbelt、Linuxのbubblewrap、WindowsのAppContainerに下位のプロセス起動処理がある。サーバー向けにデスクトップ不要のポリシーと標準入力の接続を実装・検証する必要がある。
- 既存の共通プリセットはJava/libraries/assets等の読取と、game/tmp等への書込を組み立てる。サーバー用の書込先はこの前提から個別に定義する。
- 現在のネットワーク設定は `Denied` / `Internet` の2択。許可時は外向通信・待受通信・LAN通信をまとめて許可する。特定ポートだけの待受や外向通信だけの禁止は現行モデルで表現できない。
- ネットワークを許可してもOSファイアウォールやネットワーク構成による到達性は別。Windowsのloopback例外も既存コードは自動追加しない。
- 既存MonaLauncherではサンドボックス利用不可時に通常起動へ自動fallbackしない。
- 既存の検証記録にはmacOS/Linuxのクライアント起動と制限プローブがあるが、この計画作成中には実行していない。Windows CIの直近成功ログやサーバー接続の実証も今回取得していない。
- 根拠: MonaLauncherの `src-tauri/src/sandbox/mod.rs`、`src-tauri/src/platform/{macos,linux,windows}/`、`docs/sandbox-policy.md`。

## 参照した一次資料

- Modrinth API: https://docs.modrinth.com/api/
- Modrinth version: https://docs.modrinth.com/api/operations/getversion/
- Modrinth環境分類: https://modrinth.com/news/article/new-environments/
- GitHub release assets: https://docs.github.com/en/rest/releases/assets
- Sinytra Connector: https://github.com/Sinytra/Connector
- Forgified Fabric API: https://github.com/Sinytra/ForgifiedFabricAPI

Modrinthの旧project側 `client_side` / `server_side` のみを前提にせず、採用するAPIで実際に返るversion環境情報を確認する。Connectorの依存はMinecraftの対象版ごとに確認する。

## 実装状況

- パッケージ管理ライブラリとCLIを実装中。共有構成、両側の解決、ハッシュ固定、ignore、キャッシュ、復旧ジャーナル、対話検索、JSON出力を追加した。
- macOSでRustテスト9件とClippyが成功。実Modrinth APIで検索、Sodium・Iris・Lithiumの導入、両側のハッシュ、オフライン別環境再現、ignore依存検証、手動変更の復元、HTTPS URL導入・削除を確認した。
- 上記はパッケージ管理の検証。初版の完了条件であるJava準備、Fabricの両側起動・接続、サンドボックス強制は引き続き実装・検証が必要。
- Paper、Connector、MonaLauncherへの接続、権限メタデータは合意した順序で後続段階として扱う。
